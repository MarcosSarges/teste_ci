{
  "name": "teste_ci",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test:ci": "jest --ci --coverage --maxWorkers=80%"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  },
  "jest": {
    "collectCoverageFrom": [
      "<rootDir>/packages/**/*.{tsx,ts}",
      "!**/*.{js,d.ts}"
    ],
    "coverageReporters": [
      "lcov",
      "json-summary"
    ]
  }
}
